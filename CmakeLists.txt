# ------------------------------------------------------------------------------
# CMakeLists.txt - Dummy CMake configuration for CLion indexing
#
# This file is NOT used for building the project.
# The actual build is handled by the provided Makefile using devkitARM.
#
# This dummy CMake setup is here only to enable:
#   - Code navigation (go to definition, symbol search, etc.)
#   - Code completion and syntax highlighting
#   - Project-wide indexing in CLion
#
# It uses the native compiler (e.g. clang) and does not attempt to compile
# for the actual 3DS target platform.
#
# Do not attempt to build using this configuration.
# ------------------------------------------------------------------------------
cmake_minimum_required(VERSION 3.10)
project(netpass C)

# Set the path to devkitARM (adjust this for your system)
set(DEVKITARM "$ENV{DEVKITARM}")

include_directories(
        ${CMAKE_SOURCE_DIR}/source
        ${DEVKITARM}/arm-none-eabi/include
        $ENV{DEVKITPRO}/libctru/include
)

#file(GLOB_RECURSE SOURCES source/*.c)

# Collect source files (adjust paths if needed)
file(GLOB_RECURSE SOURCES
        ${CMAKE_SOURCE_DIR}/source/*.c
        ${CMAKE_SOURCE_DIR}/source/*.h
)

add_executable(${PROJECT_NAME} ${SOURCES})